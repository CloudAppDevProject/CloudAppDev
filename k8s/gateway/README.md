# Install: 
To create the pods and setup the api gateway enter the following command: \
`helm install api-gateway <relative path to gateway folder>`

The release name ("api-gateway") is important for the frontend to find this service

---

To upgrade / update the installation do: \
`helm upgrade api-gateway <relative path to gateway folder>`