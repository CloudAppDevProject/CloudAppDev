apiVersion: v1
kind: Service
metadata:
  name: cloudappdev-frontend
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: cloudappdev-frontend
  ports:
    - protocol: TCP
      name: frontend-svc-port
      port: 80
      targetPort: frontend-port