{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "itinerary.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "itinerary.labels" . | nindent 4 }}

  annotations:
    iam.gke.io/gcp-service-account: "itinerary-{{ $.Values.namespace }}-sa@cloudappdev-dev.iam.gserviceaccount.com"
{{- end }}
