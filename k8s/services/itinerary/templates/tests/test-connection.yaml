apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itinerary.fullname" . }}-test-connection"
  labels:
    {{- include "itinerary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itinerary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
