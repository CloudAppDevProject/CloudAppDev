{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "social.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "social.labels" . | nindent 4 }}

  annotations:
    iam.gke.io/gcp-service-account: "social-{{ $.Values.namespace }}-sa@cloudappdev-dev.iam.gserviceaccount.com"
{{- end }}
